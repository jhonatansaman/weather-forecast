export type ConvertFullDate = {
  dayText: string;
  dayNumber: number;
  monthText: string;
  yearNumber: number;
  fullDate: string;
};

export type ConvertHour = {
  fullHour: string;
  hour: number;
};
